let layoutData;
let name = 0;
let path = require('path');

module.exports = function(source) {
	console.log('-- post loader');
	// console.log(source);
	// name++;
	// if (layoutData == false) {
	// 	let layoutData = '{';
	// }

	// layoutData += "'" + name + "': `" + source + "`,";

	// return layoutData + '}';
}
